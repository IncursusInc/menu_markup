fields:
  link_markup:
    label: 'Link Markup'
    description: 'HTML markup for this menu link. Special tokens: @title (link text), @nodeCount (node count if a content type is selected below). The link itself will be created for you, so there is no need for an anchor tag.'
  node_count_type:
    label: 'Node Count Type'
    description: 'If selected, the special token @nodeCount may be used above in the link markup to show a tally.'
